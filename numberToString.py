# Convert a Number to a String

def number_to_string(num):
    return str(num)

# Get the number as input and convert it to an integer
num = int(input("Enter a number: "))

# Call the function and print the result
result = number_to_string(num)
print(result)
